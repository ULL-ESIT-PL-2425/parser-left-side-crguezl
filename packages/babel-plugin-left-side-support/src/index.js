const { functionObject, FunctionObject } = require("./function-object");
const { assign, mapAssign } = require("./assign");

module.exports = {
  assign,
  mapAssign,
  functionObject,
  FunctionObject,
};
