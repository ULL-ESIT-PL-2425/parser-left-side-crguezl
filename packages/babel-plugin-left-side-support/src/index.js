const { functionObject, FunctionObject, Storage } = require("./function-object");
const { assign } = require("./assign");

module.exports = {
  assign,
  functionObject,
  FunctionObject,
  Storage
};
