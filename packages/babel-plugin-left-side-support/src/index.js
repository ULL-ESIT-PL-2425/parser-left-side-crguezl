const { functionObject, FunctionObject } = require("./function-object");
const { assign, mAssign } = require("./assign");

module.exports = {
  assign,
  mAssign,
  functionObject,
  FunctionObject,
};
